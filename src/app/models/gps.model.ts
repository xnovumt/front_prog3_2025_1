export class GPS {
    id?: number;
    latitud?: number; // Updated to match form input type
    longitud?: number; // Updated to match form input type
    maquina_id?: number; // Renamed to match backend
}
