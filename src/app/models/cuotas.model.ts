import { Factura } from "./factura.model";
import { Servicio } from "./servicio.model";

export class Cuotas {
    id?: number;
    id_servicio?: number;
    valor?: number;
    created_at?: Date;
    updated_at?: Date;
}
